{
  "name": "fogbg_vue",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "set NODE_OPTIONS=--openssl-legacy-provider && vite preview",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && vite build",
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && vite",
    "lint": "set NODE_OPTIONS=--openssl-legacy-provider && eslint --ext .js,.vue --ignore-path .gitignore --fix src"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.3",
    "jquery": "^3.7.1",
    "vue": "^3.4.23",
    "vue-router": "^4.3.2"
  },
  "devDependencies": {
    "caniuse-lite": "^1.0.30001611",
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.8"
  }
}
